import { sprinkles } from "../../../styles/sprinkles.css";

export const recipeItem = sprinkles({
  border: "default",
  borderRadius: "lg",
  marginY: 1,
  marginX: 2,
  fontFamily: "sans",
});

export const itemLink = sprinkles({
  display: "block",
  paddingY: 1,
  paddingX: 2,
});

export const recipeList = sprinkles({
  margin: 0,
  padding: 0.5,
  listStyle: "none",
});

export const heading = sprinkles({ margin: 0, padding: 0 });

export const date = sprinkles({ margin: 2 });
