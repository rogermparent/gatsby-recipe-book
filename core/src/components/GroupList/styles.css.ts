import { sprinkles } from "../../styles/sprinkles.css";

export const list = sprinkles({
  paddingLeft: 6,
  margin: 0,
  fontFamily: "sans",
});

export const item = sprinkles({});
export const itemLink = sprinkles({ padding: 1, display: "block" });
